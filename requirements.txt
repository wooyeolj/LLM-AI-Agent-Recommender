# Python 패키지 목록
# pip install -r requirements.txt

# 웹 프레임워크 및 서버
fastapi==0.136.0          # 서버 프레임워크
uvicorn[standard]==0.44.0 # FastAPI 실행 서버
pydantic==2.13.3          # 데이터 검증
pydantic-settings==2.14.0 # .env 파일 > Python 설정 객체 변환
streamlit==1.56.0         # UI 프레임워크

# HTTP 통신
httpx==0.28.1             # 비동기 HTTP 클라이언트 (Ollama, HuggingFace, GitHub API 호출)
requests==2.32.3          # 동기 HTTP 클라이언트 (Streamlit 프론트엔드, 통합 테스트)
python-dotenv==1.2.2      # .env 파일 로드

# AI 모델
sentence-transformers==5.4.1  # 임베딩(BGE-M3-ko) + 리랭커(CrossEncoder)
chromadb==1.5.8               # 벡터 DB (임베딩 저장 및 유사도 검색)

# 데이터 처리
pandas==2.3.3             # 결과 표 출력 

# PyTorch CPU 전용
torch==2.11.0 --index-url https://download.pytorch.org/whl/cpu

# 요청 속도 제한
slowapi==0.1.9          # FastAPI용 속도 제한

# 개발/테스트 도구
pytest==8.3.5           # 테스트 프레임워크
pytest-asyncio==0.24.0  # async def 테스트 지원